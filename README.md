# BackTCG

## Development server

RUN npm i

## To run locally
RUN docker-compose up

RUN yarn local

Backend available at localhost:3000
DB available at localhost:8001

## To run unit tests
RUN yarn test